[package]
name = "game-core"
version = "0.1.0"
edition = "2021"

# Pure: no wasm-bindgen, no spacetimedb. Stays deterministic and testable in isolation.
[dependencies]
serde = { workspace = true }

[dev-dependencies]
rand = { workspace = true }
rand_chacha = { workspace = true }
