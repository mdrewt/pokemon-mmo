//! Overworld domain: the tile map, the movement rule, and NPC wander logic — the systems that
//! drive characters around a map. All pure and deterministic (see crate docs).

pub mod map;
pub mod movement;
pub mod npc;
