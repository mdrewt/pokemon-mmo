# Open-source library
- Public API is a contract: SemVer strictly; breaking change = major + ADR.
- Minimal surface; document every public item; doctests/examples that run in CI.
- Zero required heavy deps where avoidable; clear MSRV/engines.
- README with quickstart; CHANGELOG generated; CONTRIBUTING + license present.
