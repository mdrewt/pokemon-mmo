# Copy to `frontend/.env` (gitignored) and adjust for your environment.
# SpacetimeDB connection used by the frontend networking layer.
VITE_SPACETIME_URI=ws://127.0.0.1:3000
VITE_MODULE_NAME=monster-tamer-mmo
